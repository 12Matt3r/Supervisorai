---
name: universal-multi-agent-orchestrator
display_name: Universal Multi-Agent Orchestrator v3.1
version: 3.1.0
description: Production-grade pure-Bash sub-agent orchestrator with JIT skill bridge, 8-Invariant envelopes, adversarial shielding, and continuity enforcement. Ships with 30+ static agents.
author: MiniMax Agent
license: MIT
language: bash
entry: skill.sh
category: agent-framework
tags:
  - orchestrator
  - multi-agent
  - sub-agent
  - supervisor
  - dispatcher
  - jit
  - adversarial
  - agentic
  - bash
test: bash test_v3_master.sh
requirements:
  bash: ">=4.0"
  jq: ">=1.6"
  python3: ">=3.8"
capabilities:
  - agent_dispatch
  - agent_supervision
  - jit_skill_acquisition
  - adversarial_shielding
  - continuity_enforcement
  - async_yielding
  - prompt_inheritance
  - static_linting
  - consensus_voting
  - memory_hydration
---

# Universal Multi-Agent Orchestrator v3.1 — JIT-Native Edition

## Overview

A production-grade, pure-Bash sub-agent orchestration skill for planning, dispatching, supervising, and verifying parallel AI agents over a unified 8-Invariant envelope.

## Quick Start

```bash
# Run the master acceptance test suite (6 phases, all green)
bash test_v3_master.sh

# List all available agents
bash skill.sh --agents-discover

# Static-lint every agent against the 8 Invariants
bash skill.sh --agents-lint --all

# Compile a prompt inheritance tree
bash skill.sh --agents-compile <agent_name>

# Dispatch a task through the V3 pipeline
bash skill.sh --dispatch-v3 <task_id> <agent_role>

# Trigger the JIT skill bridge manually
bash skill.sh --jit-bridge "<capability>"

# Inspect JIT request/response logs
bash skill.sh --jit-status
```

## Key Features

- **8-Invariant envelope** — every agent JSON must declare I1–I8 (resources, writes, reads, schema, quality, composability, versioning, deprecation).
- **Just-In-Time Skill Bridge** — three-tier resolution: local agents → native tool library → web-research synthesis.
- **Adversarial & Continuity Gates** — pre-dispatch injection detection plus post-dispatch rule enforcement.
- **Async Deep Sleep** — agents can `cmd_task_yield` to persist scratchpad to `memory/suspended/` and exit 202.
- **Prompt Inheritance Trees** — `--agents-compile` merges trait files (aesthetic, persona) into compiled prompts.
- **Master Test Suite** — `test_v3_master.sh` mathematically proves every gate, loop, and stub works.

## CLI Reference

| Command | Purpose |
|---|---|
| `--agents-discover` | List all agents + invariants |
| `--agents-lint --all` | 8-Invariants static check |
| `--agents-compile <name>` | Compile prompt inheritance tree |
| `--dispatch-v3 <task> <agt>` | Full V3 pipeline |
| `--jit-bridge <capability>` | Trigger JIT bridge manually |
| `--jit-status` | Show JIT request/response logs |
| `--llm-stub` | Show which LLM stub functions are loaded |
| `--audit-show` | Show audit log events |

## File Map

| Path | Purpose |
|---|---|
| `skill.sh` | Main entry point and CLI dispatcher |
| `skill.json` | Skill manifest (MiniMax library format) |
| `manifest.json` | Alternative manifest (generic platforms) |
| `skill.yaml` | YAML manifest with frontmatter |
| `SKILL.md` | Skill documentation |
| `config.minimax.json` | Live configuration |
| `lib/` | 40+ bash library modules |
| `lib/v3.sh` | V3 architecture (hydration, adversarial, consensus, continuity, scaffold) |
| `lib/jit_bridge.sh` | Just-in-time skill acquisition |
| `lib/llm_stubs.sh` | Native LLM heuristic stubs |
| `lib/async.sh` | Async deep-sleep yielding |
| `agents/` | 30+ static agent JSON envelopes |
| `test_v3_master.sh` | 6-phase acceptance test suite |

## Test Suite (6 Phases — All Green)

```
[TEST] Phase 1: Static Architecture & Discovery    [PASS]
[TEST] Phase 2: Native LLM Inference Stubs         [PASS]
[TEST] Phase 3: The Continuity Engine              [PASS]
[TEST] Phase 4: The JIT Skill Bridge               [PASS]
[TEST] Phase 5: Asynchronous Yielding              [PASS]
[TEST] Phase 6: Compile Inheritance Trees          [SKIP]

SUCCESS: ALL SYSTEMS NOMINAL.
```

## Author

MiniMax Agent

## License

MIT
